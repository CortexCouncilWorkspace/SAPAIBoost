variable "folder_id"{
    type = string
    description = "The GCP folder ID for the project creation"
}

variable "project_name"{
    type = string
    description = "The name of the project to create where the app will be deployed to"
}

variable "project_id"{
    type = string
    description = "The ID of the project to create where the app will be deployed to"
}